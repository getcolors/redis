# -*- mode: sh; sh-shell: bash -*-
export DIRENV_WARN_TIMEOUT=20s
eval "$(devenv direnvrc)"
use devenv 2>/dev/null
# Credentials belong only in the ignored .envrc.private. Never export
# COLORS_PAR_PROFILE: the package refuses that state-redirection overlay.
source_env_if_exists .envrc.private
